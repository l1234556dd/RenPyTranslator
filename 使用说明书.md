# RenPy 翻译工具 · 使用说明书

一款本地 Windows 桌面工具，对 Ren'Py 引擎的视觉小说游戏做**实时游戏内翻译**。边玩边翻，译文自动缓存，同句第二次秒出。

---

## 一、快速开始（3 步）

1. **双击 `RenPyTranslator.exe`** 打开软件。
2. **选游戏目录**：点"浏览…"选游戏根目录（含 `.exe` 的那一层），或点"指定启动程序…"直接选游戏 exe。
3. **配引擎 + 填 Key**：
   - 在"服务商预设"下拉框选你的服务商（默认 DeepSeek）；
   - 填 **API Key**；
   - 目标语言默认「中文（简体）」，可下拉切换其他语言。
4. 点 **「启动游戏并翻译」**，软件会自动：启动游戏 → 注入 → 实时翻译。

> 第一次遇到某句对话时，LLM 需要 2~8 秒翻译（会先显示原文，几秒后自动变中文）；已翻译过的句子秒出中文。

---

## 二、功能详解

### 1. 实时翻译
游戏运行时逐句翻译，译文直接替换显示在游戏内。翻译结果自动缓存，跨会话永久复用。

### 2. 批量预翻译（推荐，全中文无延迟）
- 先点 **「提取文本」**（需游戏运行中），把游戏全部文本导出来；
- 再点 **「批量翻译…」** 选择导出的 JSON，工具会用多线程把全部文本翻进本地缓存；
- 之后进游戏，所有句子**直接显示中文，不再等 LLM**。

### 3. 术语表
固定专有名词的译法（人名、地名、招式名等）。点「添加词条」填入原文和译文，翻译时会优先套用。

### 4. 多语言目标
"目标语言"下拉框支持：中文（简体）、繁体中文、英文、日文、韩文、俄语、西班牙语、法语、德语。

### 5. 用量统计
实时显示：
- **总 token**（输入 + 输出）
- **估算费用**（按下方两个单价算，可自行修改成你服务商的真实价格）
- **缓存命中 / 未命中 / 命中率**

### 6. 多服务商预设
内置 DeepSeek / OpenAI / 通义千问 / Kimi / Ollama(本地) / 自定义，切换自动填充接口地址和模型名。这些都是 OpenAI 兼容接口，API Key 填各自服务商的即可。

---

## 三、常见问题

### Q1：游戏里中文显示成"口口口"（豆腐块）
工具已自动处理：首次启动游戏时会自动把中文字体（黑体）复制进游戏的 `game/fonts/` 目录，并替换游戏字体。若仍出现口口，关闭游戏重开一次即可。

### Q2：翻译太慢，每句都要等
LLM 翻译本身有 2~8 秒延迟，这是所有 LLM 翻译工具的通病。解决办法：用**「批量预翻译」**提前把所有句子翻进缓存，进游戏就全中文了。

### Q3：换了游戏，缓存会混吗？
不会。**每个游戏的翻译缓存单独存一个文件夹**（按游戏目录自动区分），互不污染。

### Q4：费用单价怎么改？
在"用量统计"区有两个输入框（输入单价、输出单价，单位：元 / 百万 token），改成你服务商的实际价格即可，改完自动保存。

### Q5：关闭软件后翻译缓存还在吗？
在。缓存存在本地磁盘（`%APPDATA%\RenPyTranslator\games\<游戏标识>\cache.db`），跨会话永久保留。

---

## 四、注意事项

- 工具会向游戏进程注入翻译代码，杀毒软件可能误报，请添加信任/排除。
- 请仅用于个人学习与合法汉化用途，汉化内容版权归原游戏厂商所有。
- API Key 会明文存在本机配置文件，请勿把 `config.json` 上传到公开仓库。
